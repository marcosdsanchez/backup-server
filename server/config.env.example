### Server Configuration
DISK="/dev/sda"
HOSTNAME="backup-server"
TIMEZONE="UTC"
LOCALE="en_US.UTF-8"

# Secondary user configuration (Optional)
# If empty, no secondary user will be created and root login via key will be preferred.
ADMIN_USERNAME="admin"

# EFI partition size
EFI_SIZE="512M"
CRYPT_NAME="cryptroot"

# SSH public key for remote unlocking (initramfs) and root access
# Recommendation: Paste your public key here or point to a file
SSH_PUBKEY=""
